using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class Enum{
    public enum Path{
        Start, Path, End
    }
    public enum Dir{
        Null, Start, End, North, East, South, West,
    }
}